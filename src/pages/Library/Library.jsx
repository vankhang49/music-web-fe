import {Container, Typography} from "lvq";

function Library(props) {
    return (
        <Container withShadow={false}>
            <Typography tag="h1">Thư viện</Typography>
        </Container>
    );
}

export default Library;